# Release v0.1.0

Initial release of `gocommit`.

## Highlights
- Generates commit message suggestions from `git diff --staged` via Ollama.
- Lets you pick a suggestion or edit manually.
- Supports env overrides (`OLLAMA_ENDPOINT`, `OLLAMA_MODEL`).
- CI + tests included.

